import { api } from './api';

export const authService = {
  login: async (email: string, password: string) => {
    // Login logic
  },
  register: async (userData: any) => {
    // Register logic
  },
  logout: async () => {
    // Logout logic
  },
};