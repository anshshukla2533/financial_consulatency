import { api } from './api';

export const caService = {
  getCAs: async () => {
    // Get CAs logic
  },
  getCAProfile: async (id: string) => {
    // Get CA profile logic
  },
  requestCA: async (caId: string, itrId: string) => {
    // Request CA logic
  },
};